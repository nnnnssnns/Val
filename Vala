<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Be My Valentine</title>
    <style>
        /* Import simple, spaced-out Google font */
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: url('https://www.transparenttextures.com/patterns/heart.png');
            background-size: 30px;
            background-color: #ff9a9e;
            font-family: 'Poppins', sans-serif;
            text-align: center;
            overflow: hidden;
        }

        /* Background teddy bear faded */
        .background-teddy {
            position: fixed;
            width: 100%;
            height: 100%;
            font-size: 200px;
            color: rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: -1;
        }

        /* Main container */
        .container {
            position: relative;
            margin-top: 50px;
        }

        /* Beating Heart replaces teddy bear */
        .heart-beating {
            font-size: 100px;
            color: red;
            position: relative;
            animation: beat 1s infinite;
            z-index: 10;
        }

        @keyframes beat {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.5); }
        }

        /* Valentine Text (simplified font, spaced) */
        h1 {
            font-size: 2.5em;
            letter-spacing: 5px;
            color: white;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
            margin-top: 20px;
            text-transform: uppercase;
            font-weight: normal;
        }

        /* Button Container */
        .buttons-container {
            margin-top: 20px;
        }

        .yes-no-button {
            padding: 10px 20px;
            margin: 0 10px;
            font-size: 1.5em;
            cursor: pointer;
            background-color: #ff4c4c;
            border: none;
            color: white;
            border-radius: 25px;
            transition: background-color 0.3s;
        }

        .yes-no-button:hover {
            background-color: #e44a4a;
        }

        /* Floating hearts */
        .hearts-container {
            position: fixed;
            width: 100%;
            height: 100%;
            pointer-events: none;
        }

        .heart {
            position: absolute;
            color: red;
            font-size: 24px;
            animation: float 5s linear infinite;
        }

        @keyframes float {
            0% { transform: translateY(0) scale(1); opacity: 1; }
            100% { transform: translateY(-100vh) scale(1.5); opacity: 0; }
        }

        /* Page Changes */
        .crying-emoji, .message-page {
            display: none;
            text-align: center;
        }

        .crying-emoji.show {
            display: block;
        }

        .message-page.show {
            display: block;
            max-height: 80vh;
            overflow-y: scroll;
            padding: 20px;
            text-align: center;
        }

    </style>
</head>
<body>
    <!-- Background Teddy -->
    <div class="background-teddy">🐻</div>

    <div class="container">
        <div class="heart-beating">❤️</div>
        <h1>Petronilla, will you be my Valentine?</h1>
        
        <div class="buttons-container">
            <button class="yes-no-button" id="yes-button">❤️ Yes</button>
            <button class="yes-no-button" id="no-button">❌ No</button>
        </div>
    </div>

    <div class="hearts-container"></div>

    <!-- Crying Emoji Page -->
    <div class="crying-emoji">
        <h1>😢 I'm sorry...</h1>
    </div>

    <!-- Message Page -->
    <div class="message-page">
        <h1>Petronilla, thank you for being in my life, for loving me, and for being my girl, I'll forever cherish you and love you till the end ❤❤❤</h1>
    </div>

    <script>
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('heart');
            heart.innerHTML = '❤️';
            document.querySelector('.hearts-container').appendChild(heart);
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.animationDuration = Math.random() * 3 + 2 + 's';

            setTimeout(() => {
                heart.remove();
            }, 5000);
        }

        setInterval(createHeart, 300);

        // Yes button - Show message page
        document.getElementById('yes-button').addEventListener('click', function() {
            document.querySelector('.container').style.display = 'none';
            document.querySelector('.crying-emoji').classList.remove('show');
            document.querySelector('.message-page').classList.add('show');
        });

        // No button - Show crying emoji page
        document.getElementById('no-button').addEventListener('click', function() {
            document.querySelector('.container').style.display = 'none';
            document.querySelector('.crying-emoji').classList.add('show');
            document.querySelector('.message-page').classList.remove('show');
        });
    </script>
</body>
</html>
